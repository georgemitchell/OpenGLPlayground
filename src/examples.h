#ifndef EXAMPLES_H
#define EXAMPLES_H

#include <GLFW/glfw3.h>

int run_shaders(GLFWwindow* window);
int run_textures(GLFWwindow* window);
int run_transformations(GLFWwindow* window, GLfloat resolution_ratio, float mix_value);
int run_coordinate_systems(GLFWwindow* window, GLfloat resolution_ratio, float mix_value);


#endif
